= form_for [@product, @comment], class: "form-group" do |f|
  %p
    %label
      = f.label :content, "Leave Comment:"
      = f.text_area :content, class: "form-control"
    %p
      = f.submit "Comment", class: "btn btn-default"
